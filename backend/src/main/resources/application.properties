spring.application.name=family-finance
# 本地数据库口令等本机差异配置见 application-local.properties（已被 .gitignore 排除，不提交）
spring.profiles.active=local

# ---- 数据源（本地 MySQL 8，库由 sql/init.sql 创建；用户名/口令在 local 配置覆盖） ----
spring.datasource.url=jdbc:mysql://localhost:3306/family_finance?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=

# ---- MyBatis-Plus ----
mybatis-plus.mapper-locations=classpath*:/mapper/**/*.xml
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl
mybatis-plus.global-config.db-config.logic-delete-field=deleted
mybatis-plus.global-config.db-config.logic-delete-value=1
mybatis-plus.global-config.db-config.logic-not-delete-value=0

# ---- Sa-Token ----
sa-token.token-name=Authorization
sa-token.timeout=86400
sa-token.is-concurrent=false
sa-token.is-share=false
sa-token.token-style=uuid

# ---- 服务端口 ----
server.port=8080
